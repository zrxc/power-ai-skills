# 插件扩展规则
## 设计原则

- 插件职责单一，一个文件只做一类注册
- 统一暴露 `setupXxx` 方法，不直接暴露零散副作用
- 需要配置的插件必须支持显式传参

## 实现要求

- 避免在模块顶层直接操作 `window` 或立即注册
- 明确依赖项，必要时补齐注释说明调用顺序
- 若新增全局方法或实例属性，补齐类型声明入口

## 验证点

- 消费项目调用一次 setup 是否能正常生效
- 未调用 setup 时是否不会产生副作用
- 插件入口和包入口是否都已导出
